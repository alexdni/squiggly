# Docker Deployment Environment Variables
# Copy this file to .env.docker and customize

# ===========================================
# Required Configuration
# ===========================================

# Admin user credentials (REQUIRED)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=your-secure-password

# ===========================================
# Mode Selection (set automatically by Docker)
# ===========================================

DEPLOYMENT_MODE=docker
DATABASE_MODE=postgres
STORAGE_MODE=local
AUTH_MODE=local

# ===========================================
# Internal Configuration (usually no changes needed)
# ===========================================

# Database
DATABASE_URL=postgresql://squiggly:squiggly@localhost:5432/squiggly

# Storage
STORAGE_PATH=/data/storage

# Worker
WORKER_MODE=http
WORKER_SERVICE_URL=http://localhost:8000

# Node
NODE_ENV=production
PORT=3000

# ===========================================
# Optional Configuration
# ===========================================

# OpenAI API key for AI interpretation (optional)
# OPENAI_API_KEY=sk-...

# Feature Flags
ENABLE_ICA=true
ENABLE_RULE_ENGINE=true
ENABLE_EXPORT=true

# Upload limits
MAX_UPLOAD_SIZE=52428800

# App URL (for generating URLs, update if using custom domain)
NEXT_PUBLIC_APP_URL=http://localhost:3000
